# Код доступа на экране входа (встраивается при сборке)
VITE_CLASS_CODE=

# OpenAI API — для локальной разработки (npm run dev)
VITE_OPENAI_API_KEY=

# Для GitHub Pages: URL Cloudflare Worker-прокси (ключ хранится только на worker)
# VITE_OPENAI_PROXY_URL=https://your-openai-worker.workers.dev
